import React from 'react'

export default function page() {
  return (
    <section id="albums-page">
        {/* Albums features, the square should have a pulse laoder, in grid too */}
    </section>
  )
}
